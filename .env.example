# 复制为 .env 后填入真实值。.env 已在 .dockerignore 里，不会被打进镜像。
# 生成随机密钥：openssl rand -hex 32

# ---- LLM 与向量 ----
DEEPSEEK_API_KEY=
ZHIPU_API_KEY=

# ---- 认证 ----
# 部署必须改成本地随机长字符串，否则任何人都能伪造 token
JWT_SECRET=

# ---- 仅 docker compose 使用：MySQL 凭据 ----
MYSQL_ROOT_PASSWORD=
MYSQL_PASSWORD=
